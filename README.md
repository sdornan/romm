# Screenshots for "Sort and filter the library by game length"

Image assets referenced from the pull request for
[rommapp/romm#4232](https://github.com/rommapp/romm/issues/4232).

This branch is unrelated to the code change and is never merged; it exists only
so the pull request body can embed images. Delete it once the pull request is
closed.
